import React from 'react';

const Catalog = () => {

  return (
    <p>Create a catalog of all the brewery's products, including details like name, description, price, and image.
    Allow users to easily update product information and add new products.</p>
  );
};

export default Catalog;